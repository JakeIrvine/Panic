//
// Created by jake on 18/06/2020.
//

#include "GridCell.h"

Panic::GridCell::GridCell(wchar_t letter, SDL_Color fg, SDL_Color bg) : letter(letter), fg(fg), bg(bg) {
	// see that wasn't too difficult jake?
}
